import { ChainId, FieldsRequirements, SupportRecord, TimeString } from '../../../types';
import { IGasPriceSource, GasPriceResult, GasValueForVersion, LegacyGasPrice } from '../types';
import { IFetchService } from '../../fetch/types';
type GasValues = GasValueForVersion<'standard' | 'fast' | 'instant', LegacyGasPrice>;
export declare class ParaswapGasPriceSource implements IGasPriceSource<GasValues> {
    private readonly fetchService;
    constructor(fetchService: IFetchService);
    supportedSpeeds(): {
        [k: string]: SupportRecord<GasValues>;
    };
    getGasPrice<Requirements extends FieldsRequirements<GasValues>>({ chainId, config, }: {
        chainId: ChainId;
        config?: {
            timeout?: TimeString;
        };
    }): Promise<GasPriceResult<GasValues, Requirements>>;
}
export {};
