import { ChainId } from '../../../types';
import { BaseHttpProvider } from './base/base-http-provider';
export declare class GetBlockProviderSource extends BaseHttpProvider {
    private readonly accessTokens;
    private readonly supported;
    constructor(accessTokens: Record<ChainId, string>);
    supportedChains(): ChainId[];
    protected calculateUrl(chainId: ChainId): string;
}
export declare function buildGetBlockRPCUrl({ accessToken }: {
    accessToken: string;
}): string;
