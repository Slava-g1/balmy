import { ChainId } from '../../../types';
import { BaseHttpProvider } from './base/base-http-provider';
export declare class AlchemyProviderSource extends BaseHttpProvider {
    private readonly key;
    private readonly supported;
    constructor(key: string, onChains?: ChainId[] | 'free tier' | 'paid tier');
    supportedChains(): ChainId[];
    protected calculateUrl(chainId: ChainId): string;
}
export declare function alchemySupportedChains(args?: {
    onlyFree?: boolean;
}): ChainId[];
export declare function buildAlchemyRPCUrl({ chainId, apiKey, protocol }: {
    chainId: ChainId;
    apiKey: string;
    protocol: 'https' | 'wss';
}): string;
