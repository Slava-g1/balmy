import { ChainId } from '../../../types';
import { BaseHttpProvider } from './base/base-http-provider';
export declare class BlastProviderSource extends BaseHttpProvider {
    private readonly key?;
    private readonly supported;
    constructor(key?: string | undefined, onChains?: ChainId[]);
    supportedChains(): ChainId[];
    protected calculateUrl(chainId: ChainId): string;
}
export declare function buildBlastRPCUrl({ chainId, apiKey }: {
    chainId: ChainId;
    apiKey?: string;
}): string;
export declare function blastSupportedChains(): ChainId[];
