import { ChainId } from '../../../types';
import { BaseHttpProvider } from './base/base-http-provider';
export declare class dRPCProviderSource extends BaseHttpProvider {
    private readonly key;
    private readonly supported;
    constructor(key: string, onChains?: ChainId[]);
    supportedChains(): ChainId[];
    protected calculateUrl(chainId: ChainId): string;
}
export declare function buildDRPCUrl({ chainId, apiKey }: {
    chainId: ChainId;
    apiKey: string;
}): string;
export declare function dRPCSupportedChains(): ChainId[];
