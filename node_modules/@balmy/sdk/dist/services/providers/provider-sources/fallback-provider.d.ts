import { ChainId } from '../../../types';
import { IProviderSource } from '../types';
import { FallbackTransportConfig } from 'viem';
export type FallbackProviderSourceConfig = Pick<FallbackTransportConfig, 'rank' | 'retryCount' | 'retryDelay'>;
export declare class FallbackProviderSource implements IProviderSource {
    private readonly sources;
    private readonly config;
    constructor(sources: IProviderSource[], config: FallbackProviderSourceConfig | undefined);
    supportedChains(): number[];
    getViemTransport({ chainId }: {
        chainId: ChainId;
    }): import("viem").FallbackTransport<import("viem").Transport[]>;
}
