import { ChainId } from '../../../types';
import { BaseHttpProvider } from './base/base-http-provider';
type MoralisConfig = {
    onChains?: ChainId[];
    site?: 'site1' | 'site2';
} | {
    keys: Record<ChainId, string>;
    site?: 'site1' | 'site2';
};
export declare class MoralisProviderSource extends BaseHttpProvider {
    private readonly keys;
    private readonly supported;
    private readonly site;
    constructor(config: MoralisConfig);
    supportedChains(): ChainId[];
    protected calculateUrl(chainId: ChainId): string;
}
export declare function buildMoralisRPCUrl({ chainId, apiKey, site }: {
    chainId: ChainId;
    apiKey?: string;
    site?: 'site1' | 'site2';
}): string;
export declare function moralisSupportedChains(): ChainId[];
export {};
