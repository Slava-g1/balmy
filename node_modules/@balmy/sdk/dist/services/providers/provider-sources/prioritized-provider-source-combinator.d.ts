import { ChainId } from '../../../types';
import { IProviderSource } from '../types';
export declare class PrioritizedProviderSourceCombinator implements IProviderSource {
    private readonly sources;
    constructor(sources: IProviderSource[]);
    supportedChains(): number[];
    getViemTransport({ chainId }: {
        chainId: ChainId;
    }): import("viem").Transport;
}
