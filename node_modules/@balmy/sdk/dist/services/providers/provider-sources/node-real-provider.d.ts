import { ChainId } from '../../../types';
import { BaseHttpProvider } from './base/base-http-provider';
export declare class NodeRealProviderSource extends BaseHttpProvider {
    private readonly key;
    private readonly supported;
    constructor(key: string, onChains?: ChainId[]);
    supportedChains(): ChainId[];
    protected calculateUrl(chainId: number): string;
}
export declare function buildNodeRealRPCUrl({ chainId, apiKey }: {
    chainId: ChainId;
    apiKey: string;
}): string;
export declare function nodeRealSupportedChains(): ChainId[];
