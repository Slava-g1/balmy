import { ChainId } from '../../../types';
import { BaseHttpProvider } from './base/base-http-provider';
export declare class ThirdWebProviderSource extends BaseHttpProvider {
    private readonly supported;
    constructor(onChains?: ChainId[]);
    supportedChains(): ChainId[];
    protected calculateUrl(chainId: ChainId): string;
}
export declare function thirdWebSupportedChains(): ChainId[];
export declare function buildThirdWebRPCUrl({ chainId }: {
    chainId: ChainId;
}): string;
