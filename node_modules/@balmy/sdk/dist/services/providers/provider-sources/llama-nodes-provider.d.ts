import { ChainId } from '../../../types';
import { BaseHttpProvider } from './base/base-http-provider';
export declare class LlamaNodesProviderSource extends BaseHttpProvider {
    private readonly key?;
    private readonly supported;
    constructor(key?: string | undefined, onChains?: ChainId[]);
    supportedChains(): ChainId[];
    protected calculateUrl(chainId: number): string;
}
export declare function buildLlamaNodesRPCUrl({ chainId, apiKey }: {
    chainId: ChainId;
    apiKey?: string;
}): string;
export declare function llamaNodesSupportedChains(): ChainId[];
