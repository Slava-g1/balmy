import { ChainId } from '../../../../types';
import { IProviderSource } from '../../types';
export declare abstract class BaseHttpProvider implements IProviderSource {
    getViemTransport({ chainId }: {
        chainId: ChainId;
    }): import("viem").HttpTransport;
    abstract supportedChains(): ChainId[];
    protected abstract calculateUrl(chainId: ChainId): string;
    private assertChainIsValid;
}
