import { ChainId } from '../../../types';
import { BaseWebSocketProvider } from './base/base-web-socket-provider';
export declare class WebSocketProviderSource extends BaseWebSocketProvider {
    private readonly url;
    private readonly chains;
    constructor(url: string, chains: ChainId[]);
    supportedChains(): ChainId[];
    protected calculateUrl(chainId: number): string;
}
