import { ChainId } from '../../types';
import { Contract } from '../../shared/contracts';
export declare const MULTICALL_CONTRACT: Contract;
export declare function getViemChain(chainId: ChainId): any;
