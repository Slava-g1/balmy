import { ILogger, ILogsService, LogLevel } from './types';
export declare class LogsService implements ILogsService {
    private readonly defaultLevel;
    constructor(defaultLevel: LogLevel);
    getLogger({ name }: {
        name: string;
    }): ILogger;
}
