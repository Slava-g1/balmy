import { TimeString } from '../types';
export declare function wait(time: TimeString | number): Promise<unknown>;
export declare function waitUntil({ check, every, maxAttempts }: {
    check: () => Promise<boolean> | boolean;
    every: TimeString;
    maxAttempts?: number;
}): Promise<void>;
