import { TimeString } from '../types';
export declare class TimeoutError extends Error {
    constructor(description: string, timeout: TimeString | number);
}
export declare function timeoutPromise<T>(promise: Promise<T>, timeout: TimeString | number | undefined, options?: {
    reduceBy?: TimeString;
    description?: string;
    onTimeout?: (timeout: TimeString | number) => void;
}): Promise<T>;
export declare function reduceTimeout<T extends TimeString | number | undefined>(timeout: T, reduceBy: TimeString): T;
