export type ITriggerablePromise<T> = {
    request: () => Promise<T>;
};
export declare class TriggerablePromise<T> implements ITriggerablePromise<T> {
    private readonly trigger;
    private promise;
    constructor(trigger: () => Promise<T>);
    request(): Promise<T>;
}
